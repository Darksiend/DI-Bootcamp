const largeNumber = require("./main.js");
const b = 5;
console.log(largeNumber + b);

let http = require("http");

const server3d = http.createServer(() => {
  console.log("Im Listening");
});
server.listen(3000);
